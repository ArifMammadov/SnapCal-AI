[Unit]
Description=SnapCal API
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/snapcal/apps/api
ExecStart=/usr/bin/pnpm start
Restart=always
RestartSec=5
Environment=NODE_ENV=development

[Install]
WantedBy=default.target
