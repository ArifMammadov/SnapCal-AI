[Unit]
Description=SnapCal API - Test
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/snapcal-test/apps/api
ExecStart=/usr/bin/pnpm start
Restart=always
RestartSec=3
Environment=NODE_ENV=development
Environment=PORT=3003

[Install]
WantedBy=default.target
