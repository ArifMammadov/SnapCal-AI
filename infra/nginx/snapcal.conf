server {
    server_name snapcal.health;

    root /opt/snapcal-main/apps/mobile/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3001/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/snapcal.health/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/snapcal.health/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    server_name admin.snapcal.health;

    root /opt/snapcal-admin/apps/admin/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3005/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/snapcal.health/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/snapcal.health/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    server_name test.snapcal.health;

    root /opt/snapcal-test/apps/mobile/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3003/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/snapcal.health/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/snapcal.health/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    server_name preprod.snapcal.health;

    root /opt/snapcal-preprod/apps/mobile/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3004/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/snapcal.health/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/snapcal.health/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = snapcal.health) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name snapcal.health;
    return 404;
}

server {
    if ($host = admin.snapcal.health) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name admin.snapcal.health;
    return 404;
}

server {
    if ($host = test.snapcal.health) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name test.snapcal.health;
    return 404;
}

server {
    if ($host = preprod.snapcal.health) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name preprod.snapcal.health;
    return 404;
}
